The BMP file format for GameFrame:
8 bits per color and three colors. No alpha channel
16x16.

# Animations

Create a directory and cycle through the names 0.bmp 1.bmp etc.
Default frame rate is 200 ms
